# Example environment configuration for the insurance risk analysis project.
#
# Copy this file to `.env` and adjust the values for your local setup.
# The `.env` file should NOT be committed to version control.

# Base directories (defaults are relative to the project root if unset)
# DATA_DIR=data
# MODELS_DIR=models
# LOGS_DIR=logs

# Enable debug-style behaviour (accepted values: 1, true, yes)
# DEBUG=true
